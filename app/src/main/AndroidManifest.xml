<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lehoczky.petra.android.musicalstructureapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="NepdalokActivity"
            android:label="Népdalok" />
        <activity
            android:name="GyerekdalokActivity"
            android:label="Gyerekdalok" />
        <activity
            android:name="PoprockActivity"
            android:label="Poprock" />
        <activity
            android:name="LibraryActivity"
            android:label="Library" />
        <activity
            android:name="NowPlayingActivity"
            android:label="Now Playing">

        </activity>
    </application>

</manifest>